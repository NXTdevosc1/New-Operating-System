
- Supports Compression, fast mapping/unmapping, instant allocation/deallocation, memory reservation, large page reservation, disk paging


STRUCTS :

MAP_REGION {
	Attr, Addr, VAddr, Length;
}


FUNCTIONS :

- SetupVMem() {
	// Create clear image
	Img = CreateAllocationImage(ALLOCATED_ONLY)

}

- AllocateMem(NumPG, Attr) {
	void* mem = HmAllocate(Img, NumPG);
	MapMem(mem, mem, NumPG, Attr)
	return mem;
}

- MapMem(PhysStart, VirtStart, NumPG, Attr) {
  - Find Mem Reserved
}

- VmemAllocate(NumPG, Attr, Address) {
	void* mem = HmAllocate(AddressSpaceImg, NumPG, &Hp);
	Hp->Attributes()
	
}

- VmemCompress(Process, VStart, LengthInPages, Attr){
	
}

- DMemSave() {
	- HmSaveImage(DiskImg;
}